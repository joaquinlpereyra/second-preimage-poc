import MerkleTree from 'merkletreejs';
import { LeafSignature, LeafSourceObject, LeafValues } from '.';
export declare type WMerkleTreeOptions = {
    sortPairs?: boolean;
    sortLeaves?: boolean;
    throwOnUneven?: boolean;
};
export default class WMerkleTree<TLeafSource extends LeafSourceObject> {
    private _baseTree;
    private _leaves;
    private _sourceItems;
    private _signature;
    constructor(sourceItems: TLeafSource[], signature: LeafSignature, options?: WMerkleTreeOptions);
    get merkleTree(): MerkleTree;
    get leaves(): LeafValues[];
    get sourceItems(): TLeafSource[];
    get signature(): LeafSignature;
    hashLeafIndex(leafIndex: number): string;
    getHexProof(leafIndex: number): string[];
    getHexRoot(): string;
}
