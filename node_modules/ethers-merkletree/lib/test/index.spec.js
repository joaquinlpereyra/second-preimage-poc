"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const ethers_1 = require("ethers");
const src_1 = require("../src");
describe('index', () => {
    describe('myPackage', () => {
        it('Should correctly fetch hash Zero when fetching blank from any byte type', () => {
            const byteTypes = [
                'bytes32',
                'bytes16',
                'bytes8',
                'bytes4',
                'bytes2',
                'bytes1',
                'byte',
                'bytes',
            ];
            for (let type of byteTypes) {
                expect(src_1.merkleTreeHelper.blankFromType(type)).toEqual(ethers_1.ethers.constants.HashZero);
            }
        });
    });
});
